
# new prefix
unbind C-b
set -g prefix M-a
bind M-a send-prefix

# Use vi like keys 
set-windown-option g mode-keys vi

# True color fix
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

